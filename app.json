{
  "scripts": {
    "dokku": {
      "predeploy": "cd src && pushd pretalx/frontend/schedule-editor/ && npm install && popd && python -m pretalx rebuild"
    }
  },
  "healthchecks": {
    "web": [
      {
        "type": "startup",
        "path": "/",
        "initialDelay": 5,
        "attempts": 3
      }
    ]
  }
}
